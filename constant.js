const INTEGER = 'INTEGER', PLUS = 'PLUS', MINUS = 'MINUS', EOF = "EOF";
const LEFT = "LEFT", RIGHT = "RIGHT";
const MUL = "MUL", DIV = "DIV";
module.exports = {
    INTEGER,
    PLUS,
    MINUS,
    EOF,
    MUL,
    DIV,
    LEFT,
    RIGHT
}